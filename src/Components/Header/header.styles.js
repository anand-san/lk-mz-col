import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
  roundedButton: { borderRadius: 50, height: 50, width: 250 },
  infoContainer: { margin: 80, textAlign: "center" },
  textWhite: { color: "white" }
});
