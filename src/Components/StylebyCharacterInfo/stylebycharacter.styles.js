import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
    image: {borderRadius: "50%", border: "6px solid red"},
    roundedButton: {
        borderRadius: 50, height: 50, width: 250, margin: 50
    }
});
