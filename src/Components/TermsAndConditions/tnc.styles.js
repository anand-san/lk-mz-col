import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
  root: {
    textAlign: "left",
    margin:"50px",
    padding: "80px",
    backgroundColor: "#333",
    color: "#FFF"
  },
});
