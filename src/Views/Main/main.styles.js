import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
    longDescription: {
    textAlign: "center",
    margin:"50px",
    padding: "80px",
    backgroundColor: "#333",
    color: "#FFF",
  },
  colorRed: {
    color:"red"
  }
});
